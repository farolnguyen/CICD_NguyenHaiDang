name: Thử nghiệm CI/CD

on: 
  push:
    branches: ["main"]

jobs:
  thuc_hanh_doc_file:
    runs-on: ubuntu-latest

    steps:
      - name: Bước 1 - Xin chào
        run: echo "Hello World"
      
      - name: Bước 2 - Tải code
        uses: actions/checkout@v6

      - name: Bước 3 - Đọc file
        run: cat README.md
